# 배포 점검

2026-09-17 · 친구들에게 링크를 보내기 전 최종 점검

**https://dlthwjd02-domi.github.io/coordi-checker/**

친구들과 같은 조건 — **로컬 서버를 끄고 워커만으로** — 360 / 390 / 768 / 1280px 을 돌렸다.
**버그 3건을 찾아 고쳤다.**

## 찾은 버그

### 1. localhost 의 다른 포트에서 열면 전부 실패 (가장 큰 것)

로컬 중계를 쓸지 판단할 때 **호스트 이름만** 봤다.

```js
// 잘못된 판단
const SERVED_LOCALLY = location.hostname === 'localhost' || location.hostname === '127.0.0.1';
let relayBase = SERVED_LOCALLY ? `${location.origin}/api` : WORKER_RELAY;
```

그래서 `127.0.0.1` 의 **다른 포트**(정적 서버, 다른 도구 등)에서 열면 존재하지 않는 `/api` 를
중계로 잡고, 상품 불러오기가 모두 "불러오지 못했어요" 로 실패했다.
점검용 페이지를 8080 에서 띄우다 드러났다.

같은 출처의 `/api/health` 를 **실제로 찔러보고** 확인하도록 고쳤다.
안 되면 `127.0.0.1:8787`, 그다음 워커로 내려간다.

### 2. 360px 에서 가로 3px 넘침

`scrollWidth 363` vs 뷰포트 360. `.layout` 은 320px 인데 안의 카드가 343.5px 로 줄지 않았다.
그리드 트랙 `1fr` 의 **자동 최소값이 내용 크기**라서 생기는 전형적인 문제다.
`minmax(0, 1fr)` 로 바꿔 트랙이 줄어들 수 있게 했다 (`.layout`, `.hero-grid`).

360px 은 아이폰 SE 같은 흔한 폭이다. 그대로 두면 친구들 화면에 가로 스크롤이 생긴다.

### 3. 없는 주소가 "자동 수집 차단" 으로 안내됐다

오타(`asdfqwer`)는 403, 없는 도메인은 530 이 와서 "이 사이트가 자동 수집을 막고 있어요",
"사이트가 오류를 냈어요 (530)" 로 떴다. 워커에서 **점 없는 호스트를 먼저 거르고**,
Cloudflare 가 그 사이트에 닿지 못한 코드(521~530)는 "주소를 못 찾았어요" 로 바꿨다.

## 점검 결과

### 레이아웃

| 폭 | scrollWidth | 화면 넘침 | 내부 넘침 | 코디 보드 | 상품 카드 | JS 오류 |
|---|---|---|---|---|---|---|
| 360 | 360 | 0 | 0 | 2열 136px | 136×399 | 0 |
| 390 | 390 | 0 | 0 | 2열 151px | 151×419 | 0 |
| 768 | 768 | 0 | 0 | 4열 162px | 162×425 | 0 |
| 1280 | 1280 | 0 | 0 | 4열 159px | 159×421 | 0 |

### 기능 (워커만)

| 항목 | 결과 |
|---|---|
| 지역·기후 | 경주 10월 16.9° / 밴프 1월 -8.8°(눈 10일) / 통영 8월 27.8° |
| 상품 (유니클로 줄무늬) | 칩 기준, `stripe` 자동 태그, 아더컬러 3 |
| 상품 (유니클로 무지) | 칩 기준, 아더컬러 10 |
| 상품 (나이키) | `$42.97`, 사진 기준, 아더컬러 8 |
| 상품 (아임웹) | `79,000원`, 아더컬러 4 |
| 막힌 사이트 | 무신사·스마트스토어·쿠팡 모두 이유와 대안 안내 |
| 사진 올리기 | 잡음 배경 → 모델로 사람만 분리, 대표색 재추출 |
| 색 고르기 | 후보 3칸 + 전체보기 버튼 |
| 합성 제스처 | 끌기 1:1 정확(60px → +15.1%), 벌리기 최대 3배, 오므리기 0.25배 |
| 진단 | 97점, 항목 5개 |
| 저장·복원·삭제 | "밴쿠버 1월 코디" 이름, 복원 시 여행지·월까지 |
| 오류 입력 | 빈칸·오타·없는 도메인·404·월13 모두 한국어 한 문장 |

### 배포 상태

- 라이브 파일이 로컬과 바이트 단위로 일치 (`index.html`, `core.js`, `local.html`)
- 워커 `/health` 정상
- 저장소에 임시 점검 파일 없음, 작업 트리 깨끗

## 다음에 배포할 때

같은 순서로 돌리면 된다. 자세한 측정 방법은 [AUDIT.md](AUDIT.md) 에 있다.

1. **작업 트리와 배포 커밋 확인** — `git status`, 라이브 파일 `shasum` 비교
2. **로컬 서버 끄기** — `pkill -f server.py`. 친구들에게는 없는 것이므로 꼭 끈다
3. **데이터 흐름** — 지역·기후, 상품 4종, 막힌 사이트 3종, 오류 입력 5종
4. **UI 흐름** — 사진 올리기·누끼, 색 고르기, 합성 제스처, 진단, 저장·복원·삭제
5. **레이아웃** — 360 / 390 / 768 / 1280px 에서 `scrollWidth`, 넘치는 요소,
   터치 영역 32px 미만, 글자 11px 미만
6. **라이브 URL 로 한 번 더** — 파일 일치와 화면을 실제 주소에서 확인

크롬은 macOS 에서 창 최소 너비가 500px 이라 390px 로는 렌더링되지 않는다.
390px 짜리 iframe 안에 띄워야 미디어쿼리가 제대로 적용된다.

## 알려진 한계 (버그 아님)

- 무신사·스마트스토어·쿠팡은 웹에서 주소로 안 된다. 사진을 올리거나
  `localhost:8787` 로 열면 된다 ([WEB.md](WEB.md) 참고)
- 평평하게 놓고 찍은 옷은 누끼가 안 따진다. 사람 분리 모델이라 사람이 있어야 한다
- 누끼 모델은 WebGL 이 필요하다. 없으면 단색 배경만 지워진다
- 태블릿(768px)에서 터치 영역이 작게 측정되는 것은 `pointer:coarse` 미디어쿼리를
  헤드리스가 `fine` 으로 보고하기 때문이다. 실제 기기에서는 커진다
- 저장한 코디는 그 브라우저에만 남는다
