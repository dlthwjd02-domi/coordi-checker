# 웹 버전 — 서버 없이 돌리기

2026-09-16 · 설치 없이 링크만 누르면 되게 만든 기록

**https://dlthwjd02-domi.github.io/coordi-checker/**

## 왜 이렇게 했나

원래는 맥에서 파이썬 서버를 켜야 했다. 상품 URL에서 이미지를 가져오는 걸 브라우저가 직접
못 하기 때문이다 (CORS, 핫링크 차단). 그 서버를 무료 호스팅에 올리는 길도 있었지만
Render 무료 플랜은 15분 미사용 후 잠들어서 첫 접속에 30~60초 걸린다. "누르면 바로"가 안 된다.

그래서 **서버를 두지 않는 쪽**으로 갔다. 가져오기만 Cloudflare Worker 가 대신하고,
색 추출·패턴 판별·배경 제거는 브라우저가 Canvas 로 직접 한다. 정적 사이트라 잠들지 않는다.

```
GitHub Pages (정적)          Cloudflare Worker          쇼핑몰
 docs/index.html  ──fetch──▶  /page?u=…  ──────────▶  상품 페이지 HTML
 docs/core.js     ──<img>──▶  /img?u=&r=  ─────────▶  상품 이미지
   └ Canvas 로 색·패턴·배경 제거

 기온·지역 검색은 Open-Meteo / OpenStreetMap 을 브라우저에서 직접 호출
```

외부 API 를 브라우저에서 직접 부를 수 있는지 먼저 확인했다. Open-Meteo 세 곳
(geocoding / archive / forecast)과 Nominatim 모두 `Access-Control-Allow-Origin: *` 을 준다.
그래서 기온과 지역 검색에는 중계가 필요 없다. 쇼핑몰만 막혀 있다.

## Cloudflare Worker

`worker/src/index.js` · 137줄 · `coordi-fetch.domii.workers.dev`

경로는 둘뿐이고 아무것도 가공하지 않는다.

| 경로 | 하는 일 |
|---|---|
| `/page?u=` | 상품 페이지 HTML 을 텍스트로. 리다이렉트된 최종 주소를 `X-Final-Url` 로 함께 준다 (상대경로 계산에 필요) |
| `/img?u=&r=` | 상품 이미지 바이트. `r` 을 `Referer` 로 붙여 핫링크 차단을 통과한다 |

열린 중계로 악용되지 않게 넣은 것들.

- **호출 출처 제한** — `Origin`(없으면 `Referer`)이 허용 목록에 있어야 한다
- **사설망 차단** — `localhost`, `127.*`, `10.*`, `192.168.*`, `169.254.*`, `172.16~31.*`, `*.local`
- **크기 제한** — HTML 4MB, 이미지 12MB
- **종류 제한** — `/img` 는 `Content-Type` 이 `image/*` 이어야 한다
- **캐시** — 이미지 7일, HTML 15분 (`cf.cacheTtl`). 반복 호출이 Cloudflare 에서 끝난다

AVIF 는 요청하지 않는다. 브라우저 지원이 갈리는데 Canvas 로 읽어야 하기 때문이다.
`image/jpeg,image/png,image/webp` 만 요청해서 WebP 로 받는다.

무료 한도는 하루 10만 요청. 배포는 `cd worker && npx wrangler deploy`.

## 브라우저 엔진

`docs/core.js` · 669줄. 파이썬 `server.py` 의 로직을 그대로 옮겼다.

| 파이썬 | JS | 방법 |
|---|---|---|
| `dominant_colors` | `dominantColors` | 가운데 크롭 → 배경 픽셀 제거 → median cut + k-means |
| `analyze_pattern` | `analyzePattern` | 96×96 회색조, 축별 밝기 분산비로 줄무늬 판정 |
| `cutout` | `cutout` | 테두리 최빈색을 씨드로, 이어진 영역만 채우기로 지움 |
| `collect_images` | `collectImages` | 정규식 대신 `DOMParser`. 지연로딩 속성·srcset·JSON 덩어리까지 |
| `pick_color_set` | `pickColorSet` | URL 모양 묶음 + 갤러리/칩 힌트 점수 |
| `garment_view` | `garmentView` | 파일명 컬러웨이 코드로 칩 짝맞추기 |
| `search_places` | `searchPlaces` | 접미사 확장 + 로마자 변환 + 두 지오코더 병합 |
| `climate` | `getClimate` | 최근 3년 관측 평균. 결과는 `localStorage` 캐시 |

UI 는 이미 점검을 통과한 `static/index.html` 을 그대로 쓰고 데이터 계층만 바꿨다.
`fetch('/api/...')` 다섯 군데를 `coordi.getProduct()` 같은 함수 호출로 교체한 게 전부다.

## 옮기면서 찾은 버그 2건

둘 다 **파이썬 서버에도 있던 버그**다. 옮기는 과정에서 결과를 대조하다 드러났고 양쪽 다 고쳤다.

### 대표 사진과 같은 컬러웨이의 칩이 후보에서 사라졌다

`annotate_colors` 는 색이 거의 같은 후보를 한 장으로 합친다. 그런데 **대표 사진과 같은 색인 칩이
바로 그 대상**이 된다. 그래서 `garment_view` 가 파일명 코드가 맞는 칩을 못 찾고 엉뚱한 컬러웨이를
집었다.

| 상품 | 집어야 할 칩 | 실제로 집은 칩 |
|---|---|---|
| 무지 SUPIMA T | 회색 (07) | 딥그린 (58) |
| 레귤러치노 | 베이지 (34) | 카키 (57) |
| 줄무늬 폴로B | 갈색 (38) | 네이비 (69) |

화면에 보여줄 목록은 합치고, 칩을 고를 때는 합치기 전 전체를 쓰도록 나눴다.

### 중앙값 분할만으로는 비중 순서가 무의미하다

median cut 은 중앙값에서 쪼개므로 **상자마다 픽셀 수가 비슷해진다.** 그래서 "가장 넓은 면적을
차지하는 색"을 고를 수가 없다. Pillow 는 팔레트를 만든 뒤 모든 픽셀을 가장 가까운 팔레트 색에
다시 배정해서 센다. 그 단계를 빼먹어서 아임웹 상품 대표색이 베이지(`#d2c8ae`)가 아니라
어두운 갈색(`#413534`)으로 나왔다.

재배정을 넣어도 청바지는 `#303646` 으로 파이썬(`#494f5f`)과 많이 어긋났다. 데님처럼 그라데이션이
있으면 어느 덩어리가 최다인지 뒤집힌다. **k-means 를 4회 돌려 중심을 수렴**시키니 `#414757` 로
맞아떨어졌다.

## 파이썬과 대조

같은 상품 8종을 양쪽에 넣고 비교했다.

| 상품 | 파이썬 | 브라우저 | 태그 |
|---|---|---|---|
| 줄무늬 소프트립T | `#a0a3a4` | `#b8bcbb` | stripe / stripe |
| 줄무늬 폴로B | `#5b2f27` | `#5a2f27` | stripe / stripe |
| 줄무늬 코튼셔츠 | `#dedfdc` | `#e0e1de` | stripe / stripe |
| 무지 SUPIMA T | `#717574` | `#727675` | solid / solid |
| 스트레이트진 | `#494f5f` | `#414757` | solid / solid |
| 레귤러치노 | `#d5bd99` | `#d4bc97` | solid / solid |
| 나이키 후디 | `#19181b` | `#18181b` | solid / solid |
| 아임웹 롱슬리브 | `#d2c8ae` | `#bcb195` | solid / solid |

대표색이 몇 단계씩 다른 것은 양자화 방식 차이다 (Pillow MEDIANCUT vs 중앙값 분할 + k-means).
**색 이름과 무채색 판정은 양쪽이 같게 나온다.** 줄무늬 자동 판별도 8종 모두 일치한다.

전체 흐름도 브라우저에서 돌려 확인했다.

```
지역검색 경주 → 경주시 (대한민국 · 경상북도)
기후 10월 → 평균 16.9° 비 11일 · cloudSun
상의 → 줄무늬 소프트립T / 대표 #b8bcbb / 태그 stripe
하의 → 스트레이트진 / 대표 #414757 / 네이비
진단 → 97점 그냥 입어도 됨
합성 컷아웃 → 상의 ok / 하의 ok
색 고르기 → 후보 3칸
JS 오류 0
```

배포 후에는 라이브 파일이 로컬과 바이트 단위로 같은지(`shasum`) 확인하고,
배포된 `core.js` 를 허용된 출처에서 불러 실제로 돌려봤다 — 통영 검색, 8월 27.8°C,
줄무늬 자동 판별, 배경 제거 성공.

## 막히는 쇼핑몰과 사진 올리기

무신사 앱 공유 링크와 네이버 스마트스토어를 넣었을 때 403·429 가 떴다. 원인을 갈라 봤다.

| 주소 | 워커(클라우드 IP) | 내 맥(집 IP) |
|---|---|---|
| 무신사 상품 페이지 | 403 | 200 |
| 네이버 스마트스토어 | 429 | 307 (리다이렉트) |
| musinsa.onelink.me | 403 | 301 (리다이렉트) |

브라우저 헤더를 전부 흉내내도(`sec-ch-ua`, `sec-fetch-*`, `upgrade-insecure-requests`)
결과가 같았다.

무신사는 `robots.txt` 에 이유가 적혀 있다. 허용 목록(Googlebot·facebookexternalhit·
ClaudeBot·Yeti 등)에는 `Allow: /` 를 주고, **목록에 없는 모든 봇에는 `User-agent: * / Disallow: /`**
를 준다. 우리 워커는 그 "목록에 없는 봇" 이다. 403 은 사이트가 의도한 동작이다.

허용 목록에 있는 봇 이름을 붙이면 통과하겠지만, 남의 서비스 이름을 도용해 사이트가 걸어둔
접근 정책을 우회하는 일은 하지 않는다. **무신사는 주소로 가져오지 않는다**가 결론이다.

> 처음에는 "무신사 상품 페이지가 JS 껍데기라 어느 버전에서도 안 된다"고 적었는데 **틀렸다.**
> 그때 쓴 상품번호가 존재하지 않는 번호였고 `__NEXT_DATA__` 안에
> `"유효하지 않은 상품 입니다"` 가 들어 있었다. 그래서 og 태그가 비어 있었던 것이다.

실제 상품(`/products/6970692`)으로 다시 확인한 결과는 이렇다.

| 경로 | 결과 |
|---|---|
| 내 맥에서 직접 (로컬 버전) | **완전히 된다** — 263KB 페이지, og 태그·가격·`__NEXT_DATA__` 모두 정상 |
| 워커 경유 (웹 버전, 주소) | 403 — robots 정책대로 막힌다 |
| 워커 경유 (이미지 주소) | **된다** — `image.msscdn.net` 은 열려 있다 |

로컬 버전에서 뽑은 값: 제목 "트릴리온(TRILLION) 라이트 후디 패딩 자켓_차콜", 가격 49,990원,
대표색 `#4c4a50`(차콜), 아더컬러 9개. 웹 버전에서 같은 상품의 이미지 주소를 붙여넣으면
`#4a494f` 가 나온다 — 거의 같다.

그래서 웹 버전에서 무신사를 쓰는 길은 **사진 올리기**와 **이미지 주소 붙여넣기** 두 가지다.
안내 문구도 그렇게 적었다.

### 제목에 붙는 사이트 꼬리

무신사 og:title 이 `트릴리온(TRILLION) 라이트 후디 패딩 자켓_차콜 - 사이즈 &amp; 후기 | 무신사`
였다. HTML 기호를 풀고, 뒤의 ` | 사이트이름` 과 ` - 사이즈 … 후기` 꼬리를 떼도록 했다.
유니클로의 ` | UNIQLO KR` 도 같이 정리된다.

막힌 사이트는 주소를 넣는 순간 이유를 사이트 이름과 함께 알려준다.
앱 공유 링크를 풀어서 무신사로 판별되는 경우도 같이 잡는다.

다만 **이미지 CDN(`image.msscdn.net`)은 워커에서 잘 열린다.** 그래서 길은 두 개다.

1. 상품 사진의 이미지 주소를 붙여넣기 (데스크톱에서 우클릭 → 이미지 주소 복사)
2. **사진을 직접 올리기** — 휴대폰에서는 우클릭이 안 되니 이게 확실하다

그래서 슬롯마다 **사진** 버튼을 넣었다. 버튼(휴대폰에서 사진 앱이 열린다),
끌어다 놓기, 붙여넣기(스크린샷 찍어서 바로) 세 가지를 받는다. 파일은 `FileReader` 로 읽어
data URL 로 만들고, 그 뒤는 인터넷 없이 Canvas 에서 같은 계산을 한다.
앱에서만 보이는 상품이나 내 옷장 사진도 이 길로 된다.

주소가 막히면 오류 문구 뒤에 "옆의 사진 버튼으로 올리면 됩니다" 를 붙인다.

### 앱 공유 링크 풀기

`musinsa.onelink.me/...?af_web_dp=https%3A%2F%2Fwww.musinsa.com%2Fproducts%2F...` 처럼
앱 공유 링크는 진짜 주소를 쿼리에 담고 있다. 가져오기 전에 `af_web_dp`·`af_dp`·`deep_link_value`·
`url`·`link` 등을 훑어 http(s) 주소가 있으면 그걸 쓴다 (두 번 감싼 링크까지 3단계).
`musinsa://goods/123` 처럼 앱 전용 주소만 있으면 그대로 둔다.

### 사진에서도 줄무늬를 자동 판별

자동 태그를 색상 칩에서만 허용했더니 올린 줄무늬 사진이 무지로 잡혔다.
칩은 23~103, 무지 칩은 0.02~0.06, 모델컷의 줄무늬 옷은 0.2~0.9 였다.
그래서 **칩이면 그대로, 사진이면 신호가 8 이상일 때만** 인정하도록 했다.
합성한 줄무늬 사진은 9307 로 나와 잘 걸린다.

## 올린 사진 누끼 따기

사진을 올리면 **바로 배경을 지운다.** 두 단계로 간다.

1. **단색 배경** — 테두리에서 시작해 이어진 영역만 지운다 (모델 없이 즉시, 경계가 가장 깔끔하다)
2. **지저분한 배경** — 그때만 MediaPipe 사람 분리 모델을 받아서 실루엣을 딴다

모델은 `selfie_segmenter`(Apache 2.0). 첫 사용에만 **3.1MB**(wasm 2.8MB + 모델 0.24MB + 코드)를
받고 그 뒤로는 브라우저가 캐시한다. 지저분한 배경 사진을 올렸을 때만 받으므로,
단색 배경만 쓰는 사람은 아예 안 받는다. 상태줄에 "처음 한 번은 모델을 받아요" 라고 알린다.

| 사진 | 결과 | 표시 |
|---|---|---|
| 단색 배경 | 테두리 채우기 | 단색 배경을 지웠어요 |
| 잡음 배경 + 사람 | 모델 분리 | 모델로 사람만 분리했어요 |
| 사람 없는 사진 (평평하게 놓은 옷) | 원본 유지 | 배경을 못 지웠어요 |

### 마스크가 거꾸로 나왔다

모델이 돌아가는데 **사람이 지워지고 배경이 남았다.** `categoryMask` 의 0/1 이 사람인지 배경인지
모델 판에 따라 다르다. 라벨을 믿지 않고 **테두리를 덜 덮는 쪽을 사람으로** 판정하도록 바꿨다.
배경은 사진 테두리를 거의 다 덮고, 사람은 안 덮는다는 성질을 쓴다.

화면을 거의 다 차지하는 마스크(92% 초과)나 거의 못 잡은 마스크(5% 미만)는 쓸 수 없는 것으로 보고
원본을 그대로 쓴다. 사람이 없는 사진에 모델을 돌리면 화면 전체를 사람으로 잡는 경우가 있었다.

### 누끼에서 색을 다시 뽑는다

배경이 섞이면 대표색이 엉뚱해진다. 잡음 배경 사진의 대표색이 `#2c2929`(배경 얼룩)로 나왔다.
배경을 지운 뒤 **투명한 부분을 건너뛰고** 색을 다시 뽑으니 `#cdcfcc`(실제 옷 색)로 잡혔다.
사람 사진이면 얼굴·머리·다리가 섞이지 않게 가운데(몸통)만 본다.

## 무신사를 주소로 쓰는 길

차단은 **IP 기준**이다. 같은 주소를 집 IP 에서 받으면 무신사 200, 스마트스토어 307 이 나온다
(쿠팡은 집에서도 403). 그래서 **가져오기를 내 맥이 하면** 그 사이트들도 그냥 된다.

로컬 서버에 중계(`/api/page`, `/api/img`)와 `/api/health` 를 넣고, **로컬 서버가 웹 앱(`docs/`)을
그대로 서비스**하게 했다. `http://localhost:8787` 로 열면 같은 화면인데 무신사 주소가 들어간다.
같은 출처라 권한·CORS 문제가 없다. 색·패턴·누끼 계산은 그대로 브라우저가 하므로 구현은 하나다.

확인: 로컬에서 열어 무신사 주소 → 제목, 49,990원, 대표색 `#4a494f`, 아더컬러 10개.

### https 페이지에서 로컬을 부르는 건 막힌다

github.io 페이지가 `http://127.0.0.1:8787` 을 부르게도 해봤다. `TypeError: Failed to fetch` 가
나고 **서버 로그에 요청이 아예 안 닿았다** — 프리플라이트조차 없었다. 브라우저가 보내기 전에 막는다.

CORS 와 사설망 프리플라이트(`Access-Control-Allow-Private-Network: true`)를 다 붙여도 같았다.
헤드리스 크롬에서 `--disable-features=LocalNetworkAccessChecks` 를 주면 감지에 성공한다.
즉 **크롬의 로컬 네트워크 접근 정책**이 원인이고, 실제 브라우저에서는 사용자 권한 허용이 필요하다
(사파리는 아예 막을 수 있다).

그래서 https 페이지의 로컬 탐지는 남겨두되(허용되면 "내 맥 연결됨" 배지), 안내는
`localhost:8787` 로 열라고 적었다.

## 핏 뷰를 손가락으로

슬라이더 대신 사진을 직접 만지게 했다. Pointer Events 로 마우스·손가락을 같이 받는다.

- **한 손가락(마우스) 끌기** — 옮기기
- **두 손가락 벌리기/오므리기** — 크기 (0.25~3배)
- **트랙패드 핀치** — `ctrl+wheel` 로 오므로 그것만 확대에 쓰고, 일반 스크롤은 페이지에 넘긴다
- 슬라이더는 접어두고 "슬라이더" 버튼으로 펼친다. "처음으로" 로 되돌린다

이동량 계산이 헷갈리는 부분이 있다. `transform: translateX() translateY() scale()` 은
행렬이 `T·S` 라서 **이동은 확대율에 영향받지 않고**, 퍼센트는 **배율 적용 전 이미지 크기** 기준이다.
그래서 끌린 픽셀을 `img.offsetHeight` 로 나누면 손가락과 1:1 로 따라온다.
검증: 60px 끌었을 때 이미지 높이 397px → `+15.1%` 가 정확히 나왔다.

`touch-action: none` 을 창에 줘야 브라우저가 스크롤로 가져가지 않는다.
그만큼 사진 위에서는 페이지가 스크롤되지 않으므로, 좌우 여백에서 스크롤하면 된다.

### 단색 사진이 통째로 지워졌다

테두리 채우기는 "테두리 색으로 이어진 영역" 을 지운다. 사진 전체가 한 색이면 전부 이어져 있어서
**이미지가 통째로 사라진다.** 남은 영역이 5% 미만이면 배경을 지운 게 아니라고 보고 원본을 쓴다.

## 남은 것

- **같은 로직이 두 곳에 있다** — `server.py` 와 `docs/core.js`. 웹 쪽이 주된 구현이고
  파이썬은 로컬 실행용으로 남겼다. 한쪽을 고치면 다른 쪽도 봐야 한다.
  로컬 버전이 더 필요 없어지면 지워서 하나로 줄이는 게 낫다
- Worker 가 도미 Cloudflare 계정에 묶여 있다. 계정을 잃으면 사이트의 상품 불러오기가 멈춘다
  (기온·지역 검색은 계속 된다)
- 무신사·네이버 스마트스토어·쿠팡·에이블리는 주소로 안 된다. 사진을 올려야 한다
- 저장한 코디는 그 브라우저에만 남는다. 기기 사이로 넘어가지 않는다
- 평평하게 놓고 찍은 옷은 누끼가 안 따진다. 사람 분리 모델이라 사람이 있어야 한다
- 누끼 모델은 WebGL 이 필요하다. 아주 오래된 브라우저에서는 단색 배경만 지워진다
