# 점검 기록

2026-09-16 · `photo-match` 브랜치 전체 점검

360 / 390 / 768 / 1280px 을 실제로 렌더링해 보고, 모든 엔드포인트에 잘못된 입력을 넣어 봤다.
**오류 7건, 모바일 6건, 불편 3건**을 고쳤다.

## 점검 방법

다음에 또 점검할 때 그대로 쓸 수 있게 방법을 남긴다.

### 1. 화면 렌더링

헤드리스 크롬으로 찍는다.

```
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --disable-gpu --no-sandbox --hide-scrollbars \
  --window-size=1280,2000 --virtual-time-budget=30000 \
  --screenshot=out.png "http://localhost:8787/..."
```

**크롬은 macOS 에서 창 최소 너비가 500px 이다.** `--window-size=390` 을 줘도 뷰포트가 500 으로 잡혀서
스크린샷만 390 으로 잘린다. 그래서 처음엔 "오른쪽이 잘린다"는 잘못된 결론이 나왔다.
`--headless=old` 도, `--force-device-scale-factor=2` 도 안 통한다.

**해결**: 390px 짜리 iframe 안에 페이지를 띄우면 iframe 이 자기 뷰포트를 가지므로
미디어쿼리가 제대로 적용된다.

```html
<iframe src="/static/_audit.html" style="width:390px;height:3900px;border:0"></iframe>
```

`--dump-dom` 은 창 크기를 무시하고 항상 500px 로 돈다. 측정값은 화면에 `position:fixed` 로
그려 넣고 스크린샷으로 읽는 게 확실하다.

### 2. 자동 구동

실제 페이지를 복사해 뒤에 구동 스크립트를 붙인 `_audit.html` 을 만든다. 인라인 스크립트끼리는
전역을 공유하므로 `setPlace`, `load` 같은 함수를 그대로 부를 수 있다.

```js
setPlace({name:'밴쿠버', where:'캐나다', lat:49.25, lon:-123.12});
document.querySelector('#in-top').value = '...';
await load('top');
```

`?stack`, `?pick`, `?nobar` 같은 쿼리로 합성 모드·색 고르기·진단바 숨김을 전환했다.

### 3. 측정 항목

`window.onerror` 와 `unhandledrejection` 을 모아 JS 오류를 세고, 다음을 화면에 찍는다.

- `documentElement.scrollWidth` vs `clientWidth` — 가로 스크롤
- 모든 요소의 `getBoundingClientRect().right > 뷰포트` — 화면 밖으로 나간 요소
- `scrollWidth > clientWidth` 이면서 `overflow-x: visible` — 잘려 보이는 내부 넘침
- `#board` 의 `getComputedStyle().gridTemplateColumns` — 실제 열 수
- 버튼·입력의 높이 32px 미만 — 터치 영역
- 잎 노드의 `fontSize` 11px 미만 — 읽기 힘든 글자

### 4. 오류 입력

모든 엔드포인트에 빈 값, 헛소리, 없는 도메인, 404, 차단 사이트(쿠팡), HTML 아닌 응답,
잘못된 좌표, 월 13, 경로 탈출(`/../../etc/passwd`)을 넣어 봤다.

## 고친 것

### 오류

| | 문제 | 원인과 조치 |
|---|---|---|
| 1 | 나이키 가격이 "42.97원" | 통화를 안 봤다. `product:price:currency`·`og:price:currency`·JSON-LD `priceCurrency` 를 읽어 `$42.97` 로. 통화를 모르면 단위를 안 붙인다 |
| 2 | 오류가 영어 스택트레이스로 노출 | `HTTPSConnectionPool(host='asdfasdf', port=443): Max retries exceeded...` 가 상태줄에 그대로 떴다. `friendly_error()` 를 넣어 403/404/타임아웃/DNS 실패를 각각 다른 한국어 문구로 |
| 3 | `/api/climate` 입력 검증 없음 | 월 13 이 "기후 자료를 못 찾았어요", 빈 인자가 `could not convert string to float: ''` 로 나왔다. 좌표·월 범위를 검증한다 |
| 4 | 데님이 "차콜/그레이" | `#494f5f` 는 채도 13% 라 무채색으로 분류됐다. 채도가 낮아도 파랑기가 뚜렷하면(h 195~265, l<45) "네이비"로 부른다. **점수는 그대로** 뒀다 — 데님을 무채색처럼 보는 건 스타일링상 맞다 |
| 5 | 저장 실패를 조용히 삼킴 | `try { setItem } catch {}` 였다. 실패하면 알린다. 덧붙여 `all_candidates`(아이템마다 80장 URL)를 저장에서 제외했다 |
| 6 | 합성 모드에서 하의가 모델 상체를 보여줌 | `transform: translateY(var(--y)) scale(var(--s))` 는 **scale 이 먼저** 적용된다. 그래서 -52% 가 이미지의 32~63% 구간(허리)을 가리켰다. 다리가 보이는 -88% 로 |
| 7 | 불러오기 연타로 중복 요청 | 요청 중에는 버튼을 비활성화한다 |

### 모바일

| | 문제 | 원인과 조치 |
|---|---|---|
| 8 | 코디 보드가 1열 | `minmax(150px,1fr)` 2칸 + gap 14px = 314px 인데 컨테이너가 312px. **2px 부족해서** 1열로 떨어지고 카드가 312×585px 이 됐다. 560px 이하는 2열 고정 |
| 9 | 빈 슬롯이 사진 크기만큼 자리 차지 | `.ph` 의 `aspect-ratio:3/4`(420px) → `min-height:132px` |
| 10 | 터치 영역 22~28px | 30~34px 로. `@media (pointer:coarse)` 를 따로 둬서 태블릿도 화면 폭과 무관하게 커진다 |
| 11 | 10px 글자 (패턴 태그·예보 날짜) | 11px 로 |
| 12 | 핏 뷰 나란히가 1열 | `flex:1 1 180px` 이라 312px 에 하나만 들어갔다. 560px 이하는 `calc(50% - 7px)` |
| 13 | 합성 슬라이더가 섞여 나옴 | 2열 그리드에 컨트롤 6개라 "상의 좌우 / 하의 크기" 가 한 줄에 왔다. 1열로 |

모바일 페이지 높이 **5398 → 3941px (27% 감소)**.

| | 전 | 후 |
|---|---|---|
| board 열 (390px) | `[312px]` 1열 | `[151px 151px]` 2열 |
| 첫 카드 크기 | 312×585 | 151×415 |
| board 열 (360px) | `[282px]` 1열 | `[136px 136px]` 2열 |
| 11px 미만 글자 | 2곳 | 0 |

### 불편

| | 조치 |
|---|---|
| 14 | 저장한 코디마다 삭제(×) 버튼. 전에는 지울 방법이 없었다 |
| 15 | 코디 복원 시 여행지·월도 함께 되살린다. 이름도 "밴쿠버 9월 코디" 로 |
| 16 | 캐시가 18MB / 253개까지 쌓였다. 시작할 때 600개를 넘으면 오래된 것부터 400개로 정리 |

## 오류 입력 결과

고친 뒤 응답은 전부 한국어 한 문장이다.

| 입력 | 응답 |
|---|---|
| 빈 문자열 | 주소 형식이 이상해요. https:// 로 시작하는 상품 주소를 넣어 주세요. |
| 헛소리 · 없는 도메인 | 주소를 못 찾았어요. 사이트 주소가 맞는지 확인해 주세요. |
| 404 페이지 | 그 주소에는 상품이 없어요 (404). 주소를 다시 확인해 주세요. |
| 쿠팡 (403) | 이 사이트가 자동 수집을 막고 있어요 (403). 상품 이미지 주소를 직접 넣거나 색을 직접 지정해 주세요. |
| 이미지 없는 페이지 | 상품 이미지를 못 찾았어요. 색을 직접 지정해 주세요. |
| climate 인자 없음 · 잘못된 좌표 | 여행지를 다시 골라 주세요. |
| climate 월 13 | 월은 1~12 사이여야 해요. |
| 경로 탈출 `/../../etc/passwd` | 404 (`SimpleHTTPRequestHandler` 가 경로를 정규화해 막는다) |

## 문제 없던 것

- **JS 오류 0건** — 360 / 390 / 768 / 1280px 전부
- **가로 스크롤 0, 요소 내부 가로 넘침 0**
- 진단 점수 회귀 없음
  - 무지 7종: 93 / 87 / 86 / 79 / 76 / 64 / 52
  - 패턴 6종: 100 / 97 / 93 / 89 / 83 / 57
- 지역 검색(경주·통영·강릉·밴프·San Sebastian), 기후 조회, 배경 제거, 색 고르기 모두 정상
- 768px 은 4열, 1280px 은 원래 레이아웃 그대로

## 남은 한계

- **합성 모드 경계선** — 컷아웃이 각자 자기 경계로 잘려서 상하의 가운데가 안 맞는다.
  좌우 슬라이더로 맞춰야 한다. 어깨·허리 위치를 찾아 자동 정렬하려면 인물 검출이 필요하다
- **`pointer:coarse` 검증 못 함** — 헤드리스 크롬은 `--touch-events=enabled` 를 줘도
  `fine` 으로 보고한다. 560px 이하 경로는 검증됐으니 휴대폰에서는 확실히 커진다
- **차단 사이트** — 쿠팡·에이블리는 여전히 안 된다. 안내 문구만 나아졌다
- **바다 좌표** — `lat=0&lon=-140` 같은 태평양 좌표도 기온을 돌려준다.
  지오코더가 바다를 반환하지 않으므로 실제로는 안 걸린다



# 웹 버전

파이썬 서버 로직을 브라우저(Canvas)로 옮기고 설치 없이 쓰게 만든 기록은
[WEB.md](WEB.md) 에 있다. 옮기는 과정에서 파이썬 서버에도 있던 버그 2건을 찾아 함께 고쳤다.



# 배포 점검

친구들에게 링크를 보내기 전 최종 점검과, 다음 배포 때 그대로 따라갈 순서는
[RELEASE.md](RELEASE.md) 에 있다. 로컬 서버를 끄고 워커만으로 돌려 버그 3건을 찾아 고쳤다.
